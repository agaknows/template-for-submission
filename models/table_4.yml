version: 2

models:
  - name: table_4
    description: "Average number of products ordered per order per day"
    database: foodpanda-422109
    schema: cs_1_2
    materialized: table
    columns:
      - name: date_local
        description: date field
      - name: avg_no_of_products
        description: Average number of products ordered per order per day
